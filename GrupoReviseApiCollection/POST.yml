info:
  name: POST
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/frete/simular
  body:
    type: json
    data: |-
      {
        "cepOrigem": "20040-020",
        "cepDestino": "01310-930",
        "valorPedido": 500.00,
        "peso": 5.5,
        "tipoCarga": "GERAL"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
